---
title: "Optimization Workflow Tests"
output: 
  html_document: 
    keep_md: yes
---

This document was autogenerated at `r file.mtime("_tests.json")`

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r eval = T}
parseResults <- function(results){
  r2 <- stringr::str_split(results, '\\s+with\\s+')
  r3 <- plyr::ldply(r2, function(x){
    data.frame(
      x1 = stringr::str_match(x[1], "^Finished in (.*)"),
      x2 = ifelse(is.na(x[2]), "", x[2])
    )
  })  
  setNames(r3[,-1], c("time", "errors"))
}
d <- as.vector(jsonlite::fromJSON('_tests.json'))
r <- sapply(jsonlite::fromJSON('_results.json'), tail, 1)
#tim <- stringr::str_match(r, 'Finished in (.*) with')[,2]
if (length(d) > 0){
  df <- data.frame(
    id = seq_along(d),
    test = names(d),
    status = ifelse(sapply(d, '[[', 1) == 2, ":rage:", ":smile:")
  )
  df <- cbind(df, parseResults(r))
  rownames(df) <- NULL
  knitr::kable(df, format = 'markdown')
} else {
  cat("No test results available.\n")
  cat("Please make sure there are tests in Extras/Tests\n")
  cat("Run `runTests()` from the root folder of the project\n")
}
```
